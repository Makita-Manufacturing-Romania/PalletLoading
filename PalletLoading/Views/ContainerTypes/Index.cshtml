@model IEnumerable<PalletLoading.Models.ContainerType>

@{
    ViewData["Title"] = "Container types";
}

<h1>Container types</h1>


<link rel="stylesheet" href="~/css/background.css" />

<p>
    <a class="btn btn-success" asp-action="Create">New container type</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Length) (metrii)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Width) (metrii)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Height) (metrii)
            </th>
            <th>
                Volume (m3)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td style="padding-top:18px">
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td style="padding-top:18px">
                    @Html.DisplayFor(modelItem => item.Length)
                </td>
                <td style="padding-top:18px">
                    @Html.DisplayFor(modelItem => item.Width)
                </td>
                <td style="padding-top:18px">
                    @Html.DisplayFor(modelItem => item.Height)
                </td>
                <td style="padding-top:18px">
                    @Html.DisplayFor(modelItem => item.volume)
                </td>
                <td>
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>



   